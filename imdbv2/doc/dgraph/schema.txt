```
name: string @index(term) .
release_date: datetime @index(year) .
revenue: float .
running_time: int .
starring: [uid] .
director: [uid] .

type Person {
  name
}

type Film {
  name
  release_date
  revenue
  running_time
  starring
  director
}
```


query findMovieByPerson($full_name: string) {
	    q(func: anyof($full_name)) {
		uid
		Category.title
	    }
	}`